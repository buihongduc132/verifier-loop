# ---- Rust build artifacts ----
/target/
**/*.rs.bk
Cargo.lock
# binaries built locally
/bin/
*.exe

# ---- Coverage / profiling ----
*.profraw
*.profdata
tarpaulin-report.html
cov/
*.lcov

# ---- Sensitive / local state ----
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
secrets/
**/credentials*
**/creds*
# per-user runtime data (NEVER commit)
.verifier-loop/
*.salt
# exception: historical captured proof artifacts need their demo salt to be re-verifiable
!flow/proof/**/.salt

# ---- Editor / OS noise ----
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ---- pi local runtime (sessions, cache) ----
.pi/

# ---- Code intelligence index ----
.gitnexus/

# ---- Local notes / scratch ----
notes/
scratch/
*.local.md
_STATE.md
examples_tmp/

# ---- Dependencies vendored locally (avoid committing huge dirs) ----
/vendor/
/node_modules/
.smoke/
target

# worktrees (never commit)
.worktrees/
