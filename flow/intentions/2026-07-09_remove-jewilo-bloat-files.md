remove these files for me ; find the repository that having these as script then remove it ; ALSO , check in the script and find out what is it doing , also what are the information that it capture ; THEN put to findings for me as well; See if it is trying to cheat or something ; Then add the 1 line reference in the AGENTS.md , ALWAYS reminder user about that bloating file problem whenever you are seeing it ;
---
(elaboration) 36 `.jewilo-*` files (3 wrapper scripts + 33 generated artifacts) bloating
the CWD of `openspec-dashboard`. User wants: (1) remove them, (2) locate + remove the
generating scripts, (3) reverse-engineer what the scripts do + what info the artifacts
capture, (4) cheat/tamper assessment, (5) findings doc, (6) AGENTS.md 1-line ref, (7)
standing reminder whenever these bloat files reappear.
